<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

    </style>

    <style name="Theme.Base" parent="AppTheme">
        <item name="colorPrimary">@color/quiverPrimaryBackground</item>
        <item name="colorPrimaryDark">@color/quiverSecondaryBackground</item>
        <item name="colorAccent">@color/quiverColorBackgroundAccent</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/quiverBackgroundColor</item>

    </style>

    <style name="Theme.Quiver" parent="Theme.Base" />

    <style name="ActionBarThemeOverlay" parent="">
        <item name="android:textColorPrimary">@color/quiverPrimaryTextColor</item>
        <item name="colorControlHighlight">@color/quiverBackgroundColor</item>
        <item name="android:actionMenuTextColor">@color/quiverPrimaryTextColor</item>
        <item name="android:textColorSecondary">@color/quiverPrimaryTextColor</item>
        <item name="android:background">@color/quiverPrimaryBackground</item>
    </style>

    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/quiverPrimaryTextColor</item>
        <item name="android:textSize">18sp</item>
    </style>
</resources>
